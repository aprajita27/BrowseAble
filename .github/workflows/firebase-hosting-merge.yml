# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './browseable-caretaker/package-lock.json'
      - name: Clean install dependencies
        working-directory: ./browseable-caretaker
        run: |
          rm -rf node_modules package-lock.json
          npm install
      - name: Build
        working-directory: ./browseable-caretaker
        run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BROWSEABLE_586FA }}
          channelId: live
          projectId: browseable-586fa          
          entryPoint: ./browseable-caretaker
